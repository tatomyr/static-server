{
  "name": "static-server",
  "version": "0.0.1",
  "description": "Static server on Expess",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tatomyr",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.1"
  }
}
